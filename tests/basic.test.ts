/**
 * latte_agent 项目基础测试占位符
 * 后续开发中应添加更多针对各功能模块的单元测试
 */

import { describe, it, expect } from 'vitest'

describe('项目基础配置', () => {
  it('package.json 应该存在', () => {
    // 基础测试占位符，验证测试框架正常工作
    expect(true).toBe(true)
  })
})
